# Test Fixtures

`source/` contains small, committed text fixtures. CMake converts them into the build directory; the core program reads only generated `DRGBIN01` binaries. Downloads and run results stay under `data/` and are excluded from version control.
